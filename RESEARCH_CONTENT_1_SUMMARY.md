# 研究内容1阶段A总结：固定负荷、commitment-only 的模拟器原型

## 1. 研究目标与当前定位

研究内容1关注用量子电路辅助机组组合值函数的近似、候选搜索与真实成本验证。当前完成的是阶段A：在固定负荷窗口 u_w 下，对 commitment-only 状态 x 构建固定负荷值函数切片 Q_{u_w}(x) 的模拟器原型。

阶段A完成的是一条可审计的模拟器研究链路，而不是整个研究内容1的最终完成：训练标签来自真实 ED/LP，代理模型经相干电路写入固定点成本寄存器，严格或联合 oracle 标记候选，Grover/BBHT 在完整门级电路上搜索，候选再由真实 ED/LP 验证。

## 2. 历史 max-affine 原型

max-affine 路线是早期门级可逆值寄存器与 Grover/GAS 的探索。它验证了可逆成本寄存器、门级搜索和候选复核的原型思路，并为后续 fixed-point、comparator 与候选真实验证提供了基础，因而具有阶段性研究意义。

该原型采用独立的模型、实验设置和阈值语义。其历史结论应保留并按当时口径理解，但它不是当前 sparse-VQC + phase-to-value + joint BBHT 的正式主线。

## 3. 当前阶段A sparse-VQC 主线

当前正式模型以固定负荷窗口为条件，不把负荷作为量子输入。对每个给定 u_w，模型学习 Q_{u_w}(x)，其中 x 为机组 commitment 位串。稀疏 VQC 使用低阶、稀疏的相位项表达代理值函数，以避免把完整高阶表强行写入电路。

这一定位将“固定负荷下的值函数切片”与“面向负荷变化的通用值函数”明确区分：阶段A只验证前者。

## 4. 训练标签与真实成本

训练标签由 ED/LP 求解产生。每个 commitment x 的真实成本包含可行调度对应的运行成本；不可行状态使用明确的不可行处理规则。训练、搜索后的候选验收和 incumbent 更新均以同一真实 ED/LP 口径为准。

因此，代理成本只用于量子电路中的排序与标记条件，不能单独构成真实成本改善的证据。

## 5. phase-to-value 与 fixed-point 成本寄存器

阶段A先将稀疏 VQC 的代理输出相干地转为值寄存器中的固定点整数编码 C_hat_integer(x)。该路径包括相位表示、phase-to-value、电路内固定点量化和可逆算术，而不是在经典端直接替换量子态振幅。

该值寄存器为后续 WeightedAdder、IntegerComparator、oracle 反计算和搜索寄存器的完整门级执行提供共同接口。编码精度会带来离散化误差，因此“真实成本更低”与“编码值严格降低”在边界情形下并非同一命题；这也是最终接受规则仍必须使用真实 ED/LP 的原因。

## 6. 严格成本 oracle

当前正式成本 oracle 仅使用：

    C_hat_integer(x) < tau

    tau = encode(C_incumbent_true)

阈值 tau 来自当前真实 incumbent 的 ED/LP 成本编码。代理预测值不会直接更新 threshold；只有候选经真实 ED/LP 验证且真实成本严格改善 incumbent 时，才更新 incumbent 并据此更新 tau。

### 已撤销的 surrogate-threshold margin 探索

该探索已经撤销，不属于当前正式算法。

## 7. 硬 UC 逻辑与 joint oracle

阶段A还把硬 UC 启停逻辑以 Boolean oracle 的形式纳入搜索。联合 oracle 标记同时满足严格成本条件和逻辑可行性条件的状态。这样可在进入真实 ED/LP 前排除部分逻辑不可行候选，而不把逻辑筛选伪装成真实成本改善。

cost-only 与 joint-oracle 是两种不同的模拟器搜索设置，应在相同训练样本、真实验证口径和统计规则下比较。

## 8. Grover、BBHT 与门级测量

搜索使用完整门级 Grover 或 BBHT 电路，包括成本寄存器计算、比较器、相位标记、反计算和 diffuser。Aer MPS 模式从实际 shots 的 counts 中读取搜索寄存器与辅助寄存器测量结果；候选只能来自实际测得、且满足相应标记与验证规则的状态。

BBHT 的试探轮数、oracle 调用和候选选择过程均应记录。辅助寄存器回零概率、相位验证和 counts 映射用于检查电路实现，不替代真实 ED/LP 验证。

## 9. 真实 ED/LP 验证与 incumbent 闭环

每个被选中的候选都必须调用真实 ED/LP。仅当 candidate_true_cost 严格小于 incumbent 的真实成本时，才发生 accepted_update，并以新的真实成本更新 threshold。若量化使候选真实成本改善但编码值未改变，严格成本 oracle 的下一轮标记集合可以不变；这反映的是固定点分辨率，而不是允许用代理值修改阈值。

## 10. case14 已验收的模拟器结果

下表汇总此前已完成并验收的 10 个场景模拟器实验；本次文档整理未重新运行这些实验。

| 指标 | cost-only | joint-oracle |
| --- | ---: | ---: |
| 找到真实全局最优的场景数 | 5 / 10 | 6 / 10 |
| 平均 BBHT trials | 2.5 | 5.4 |
| 平均 oracle 调用数 | 3.7 | 9.7 |
| 平均新增精确候选尝试数 | 1.2 | 1.1 |
| 逻辑预检查拒绝数 | 2 | 0 |
| 实际 LP 求解总数 | 10 | 11 |

这些结果的可支持结论是：joint strict oracle 缩小了代理标记集合，并在真实求解器之前阻止了逻辑不可行候选，从而减少无效候选验证。它同时带来更多 BBHT trials、更多 oracle 调用及更高的门级模拟开销。上述有限场景统计不构成量子加速、必然减少 LP 调用或保证提升命中率的结论。

## 11. 资源收益与代价

联合逻辑 oracle 增加了 4 个违反条件辅助量子位。case14 的窗口 0 量子位规模约由 20 增至 24，窗口 1 约由 18 增至 22；分解后的电路深度也随逻辑约束和反计算增长。

因此，joint-oracle 的收益是更严格的候选筛选和较少的逻辑无效候选进入精确验证；代价是额外量子位、更深电路以及比 cost-only 更高的 MPS 模拟总时间。阶段A只报告这种模拟器资源权衡，不对真实硬件性能作外推。

## 12. 动态测试状态

此前的动态回归环境为 Python 3.11.15、Qiskit 2.5.0、Aer 0.17.2。研究内容1聚焦回归为 70 passed、21 warnings。完整测试当时为 102 passed、7 failed、230 warnings。

7 个失败均来自 tests/test_stage1.py 向受保护的 results/test_*.json 写入时发生 PermissionError，不是当前研究内容1算法断言失败。警告主要包括 Qiskit 弃用 API 和 pytest 缓存权限。本文档阶段未修改代码，也未重新执行测试或实验。

## 13. 当前边界

- 仅覆盖固定负荷、commitment-only 的 Q_{u_w}(x) 模拟器原型。
- 稀疏 VQC 是代理模型，真实最优性与 incumbent 改善仍由 ED/LP 决定。
- 实验结果是有限 case14 场景上的模拟器结果，不是量子硬件实验。
- 资源数据反映门级模拟的收益与代价，不能直接等同于端到端量子计算优势。

## 14. 下一阶段：负荷参数泛化

下一阶段将从固定负荷值函数切片 Q_{u_w}(x)，扩展到包含负荷输入的 Q(x,u)。届时需要重新定义训练数据覆盖范围、编码方式、泛化评估、oracle 资源评估与真实 ED/LP 验证协议；这些工作不属于当前阶段A已完成范围。
