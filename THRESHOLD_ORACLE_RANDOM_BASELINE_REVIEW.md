# 阈值 oracle 与随机选择对照：测试记录、初步结果和待解决问题

## 1. 测试问题

本记录只讨论一个问题：在固定负荷、commitment-only 的 case14 小型子空间中，joint sparse-VQC BBHT oracle 是否比随机选择更容易找到真实 ED/LP 成本严格低于当前 incumbent 的启停状态。

当前正式接受规则保持不变：

    candidate_true_cost < incumbent_true_cost

只有满足该真实 ED/LP 条件的候选才会更新 incumbent 和 threshold。量子代理成本不能直接更新 threshold。

本文是阶段A的补充测试记录，不表示研究内容1整体完成，也不构成真实量子硬件或量子加速结论。

## 2. 测试设置

源实例为 case14，时间长度为 4。因此两时段窗口只有三个合法起点：

| 窗口起点 | 时间窗口 |
| ---: | --- |
| 0 | [0, 2) |
| 1 | [1, 3) |
| 2 | [2, 4) |

标准运行使用两个可变机组、4 个 commitment 搜索比特、8 个训练样本、fractional bits 为 2、cost unit 为 1000、initialization policy 为 first。每个标准运行的上限为 24 个 BBHT trial、48 次 oracle 调用、8 次新增精确候选求值和 3 次 threshold 更新。

测试机组对包括：

- (0, 5)：基准组；
- (0, 1)：具有活跃逻辑筛选的组；
- (1, 3)：用于观察无 marked state 的组；
- (1, 5)：用于观察初始 incumbent 已最优的组。

标准 first 策略测试共 30 次：

- (0, 5)、(0, 1)、(1, 3) 各在 seed 0、1、2 和三个窗口上运行，共 27 次；
- (1, 5) 在 seed 0 和三个窗口上运行，共 3 次。

另运行 1 次 (0, 1)、窗口 0、seed 0 的扩展预算诊断，将上限提高到 48 个 trial、96 次 oracle 调用、16 次新增精确候选求值和 8 次 threshold 更新。还对 (1, 3) 与 (1, 5) 的窗口 0 做了 6 次 random 初始化诊断；这些诊断不改变正式默认策略 first。

## 3. 正式 first 策略结果

| 机组对 | 标准运行数 | 至少一次真实 threshold 下降 | 真实 threshold 更新总数 | 最终达到子空间真实全局最优 |
| --- | ---: | ---: | ---: | ---: |
| (0, 5) | 9 | 9 / 9 | 15 | 9 / 9 |
| (0, 1) | 9 | 9 / 9 | 18 | 7 / 9 |
| (1, 3) | 9 | 0 / 9 | 0 | 6 / 9 |
| (1, 5) | 3 | 0 / 3 | 0 | 3 / 3 |
| 合计 | 30 | 18 / 30 | 33 | 25 / 30 |

(0, 1) 的两个未达到最优的标准运行都在第 3 次 threshold 更新预算用尽时停止。扩展预算复跑显示，同一配置可继续完成第 4 次真实 threshold 下降，并最终达到全局最优；停止原因随后变为 48 个 trial 的预算上限。

在本批被接受的更新中，真实 threshold 和固定点编码 threshold 都同步严格下降；未观察到真实成本下降而编码 threshold 不变的量化停滞。

没有发生下降不等于代码允许伪造阈值：

- (1, 3) 的窗口 0 在 first 策略下没有代理 marked state，因而按严格规则停止；窗口 1、2 的初始 incumbent 已是该子空间全局最优。
- (1, 5) 的三个 first 策略窗口初始 incumbent 都已是全局最优，故不存在真实改善空间。

## 4. random 初始化诊断

random 初始化只从已有有限训练样本中选择真实 ED/LP incumbent，仍不使用隐藏最优解或代理值更新 threshold。

| 机组对与窗口 | random seed | threshold 更新次数 | 最终是否全局最优 |
| --- | ---: | ---: | --- |
| (1, 3)，窗口 0 | 0 | 1 | 否 |
| (1, 3)，窗口 0 | 1 | 0 | 否 |
| (1, 3)，窗口 0 | 2 | 1 | 是 |
| (1, 5)，窗口 0 | 0 | 1 | 是 |
| (1, 5)，窗口 0 | 1 | 3 | 否，更新预算停止 |
| (1, 5)，窗口 0 | 2 | 1 | 是 |

这说明 first 策略下的“无更新”有时是初始阈值和训练样本顺序的结果，不应被误读为 oracle 永远无法产生真实改善。该诊断不应取代 first 作为当前正式默认策略。

## 5. 同阈值随机基线估计

对上述 30 次标准 first 策略运行的 348 个实际 BBHT trial，使用搜索结束后仅作验证的完整真值景观，计算每一个实际 incumbent threshold 下从 16 个搜索状态均匀随机抽取一个状态时，真实成本更低状态的期望比例。

| 指标 | 数值 |
| --- | ---: |
| 实际 BBHT trial 数 | 348 |
| 实际测得并经真实 ED/LP 确认的改善次数 | 33 |
| oracle 每 trial 真实改善率 | 33 / 348 = 9.48% |
| 同阈值全空间均匀随机的期望改善次数 | 16.75 |
| 同阈值全空间均匀随机的期望改善率 | 4.81% |
| oracle 与该随机估计的比值 | 1.97 倍 |
| 辅助位合格且被标记的测量候选数 | 89 |
| marked 候选中真实改善的比例 | 33 / 89 = 37.08% |

在这个受限比较中，joint oracle 的实际真实改善率高于同一阈值条件下的全空间均匀随机估计，未显示出“比随机选择更差”的结果。

## 6. 这一比较尚不能证明什么

上节不是严格的闭环随机对照，原因如下：

1. 随机基线沿用了 oracle 实际产生的 incumbent threshold 轨迹；若随机算法独立运行，它的 incumbent 和后续阈值轨迹会不同。
2. 全空间均匀随机把硬逻辑不可行状态也包括在内，因此同时衡量了 joint oracle 的逻辑筛选和成本筛选收益，不能单独归因于 sparse-VQC 成本代理。
3. 机组对、窗口和 seed 数量有限；部分组的初始 incumbent 已最优，或不存在 surrogate marked state。
4. 一些接受更新复用了训练阶段的真实 ED/LP cache。它们仍以真实成本为依据，但不一定新增一次物理 LP 求解。

因此，1.97 倍应表述为“同阈值、离线验证下的初步富集信号”，而不是最终的量子优势或严格随机优越性结论。

## 7. 建议的严格随机对照

后续应在不改变训练集、初始 incumbent、固定点配置、最大 trial、最大新增 ED/LP 调用和停止规则的前提下，做两个独立的闭环随机候选基线：

1. 全空间随机：从 16 个 commitment 状态均匀抽样，再按同一硬逻辑和真实 ED/LP 规则验证。
2. 逻辑可行随机：只从硬 UC 逻辑可行状态均匀抽样，再按同一真实 ED/LP 规则验证。

每个机组对、窗口和初始设置应至少使用多个随机 seed。比较指标至少包括：

- 每 trial 的真实改善率；
- 每次新增 ED/LP 求值的真实改善率；
- 在固定预算内至少一次改善的概率；
- threshold 更新次数与最终真实最优性 gap；
- oracle 调用、真实 ED/LP 调用和门级模拟时间。

第一种基线衡量 joint oracle 的端到端作用；第二种基线隔离硬逻辑筛选后，检验 sparse-VQC 成本 oracle 和 BBHT 是否仍优于随机可行候选。

## 8. 原始证据位置与版本范围

本轮运行产生的 17 个 JSON 位于本地 results 目录，文件名前缀为：

    stage1_case14_2x2_joint_feasible_sparse_vqc_bbht_threshold_

它们保留完整的 threshold history、trial trace、候选真实成本、停止原因和验证数据。本次提交不包含这些 results 文件；网页端评阅应以本文件中的汇总为准，并将原始 JSON 作为本地可复核证据。

代码与本记录对应的分支为 codex/vqc-research-content1-prototype。本文档只记录实验和问题，不修改当前正式算法定义。
